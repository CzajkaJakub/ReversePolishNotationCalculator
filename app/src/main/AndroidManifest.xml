<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.reversepolishnotationcalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/calculator_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/calculator_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReversePolishNotationCalculator">

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity"/>
        <activity
            android:name=".CalculatorView"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>